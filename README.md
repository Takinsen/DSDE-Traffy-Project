# 🚀 Traffy Fondue & Air Quality Data Pipeline

โปรเจกต์ Data Engineering สำหรับรวบรวม, ประมวลผล และวิเคราะห์ข้อมูลเรื่องร้องเรียนจาก **Traffy Fondue** ร่วมกับข้อมูลคุณภาพอากาศ (**PM2.5**) เพื่อหาความสัมพันธ์และ Insight ของปัญหาในแต่ละเขตของกรุงเทพฯ

## 🏗️ Architecture Overview

ระบบออกแบบตามหลักการ **Medallion Architecture** (Bronze -> Silver Layer):

1.  **Data Source:**
    * **Traffy API:** ข้อมูลเรื่องร้องเรียน
    * **Open-Meteo API:** ข้อมูล PM2.5 ย้อนหลัง
2.  **Ingestion (Bronze Layer):**
    * **Producer:** ดึงข้อมูลและส่งเข้า **Kafka**
    * **Consumer:** รับข้อมูลจาก Kafka และบันทึกเป็น **Raw CSV** (Bronze)
3.  **Processing (Silver Layer):**
    * **Apache Spark (PySpark):** อ่าน Raw CSV
    * **Logic:** ทำ Data Cleansing, ซ่อมแซมพิกัดที่หายไป (Imputation), และจัดการ Format วันที่
    * **Output:** บันทึกเป็น **Cleaned CSV** (Silver)
4.  **Visualization:**
    * **Streamlit:** Dashboard แสดงผลข้อมูล

---

## 📥 Download Data (Reference)

**⚠️ สำคัญ:** ก่อนเริ่มโปรเจกต์ กรุณาดาวน์โหลดไฟล์ตั้งต้นและนำไปวางไว้ในโฟลเดอร์ **`data/source/`**

| ชื่อไฟล์ | รายละเอียด | ลิงก์ดาวน์โหลด |
| :--- | :--- | :--- |
| **`bangkok_traffy.csv`** | ข้อมูลเรื่องร้องเรียนย้อนหลัง (สำหรับจำลอง Producer) | [👉 คลิกเพื่อดาวน์โหลด (Google Drive)](https://drive.google.com/file/d/19QkF8i1my99gjbyHe7de_qZNwgrca6R5/view?usp=sharing) |
| **`thailand_geography.csv`** | ข้อมูลพิกัดกลางของแต่ละเขต (สำหรับ Spark ซ่อมข้อมูล) | [👉 คลิกเพื่อดาวน์โหลด (Google Drive)](https://drive.google.com/file/d/1qDtZ9ybWyR7MNyhm9pmzS8lg6KBx-X5S/view?usp=sharing) |

> **Note:** กรุณาตรวจสอบชื่อไฟล์ให้ตรงตามตารางเป๊ะๆ เพื่อให้ Script สามารถอ่านไฟล์ได้ถูกต้อง

---

## 🛠️ Prerequisites (สิ่งที่ต้องมี)

* [Docker Desktop](https://www.docker.com/products/docker-desktop/) (สำคัญมาก! ใช้รัน Kafka และ Spark)
* VS Code (แนะนำให้ลง Extension: *Python*, *Jupyter*)
* Python 3.9+ (สำหรับรัน Streamlit)

---

## 📂 Project Structure

```text
PROJECT/
├── data/
│   ├── source/             # ไฟล์ตั้งต้น (Reference)
│   │   ├── bangkok_traffy.csv
│   │   └── thailand_geography.csv
│   ├── raw/                # ข้อมูลดิบจาก Kafka (Bronze Layer)
│   │   ├── bangkok_air_raw.csv
│   │   └── bangkok_traffy_raw.csv
│   └── cleansed/           # ข้อมูลที่ผ่าน Spark แล้ว (Silver Layer)
│       ├── bangkok_air_clean.csv
│       └── bangkok_traffy_clean.csv
├── docker/
│   └── docker-compose.yml  # Config ระบบ (Spark, Kafka, Zookeeper)
├── src/
│   ├── data_source/        # Notebooks ส่งข้อมูลเข้า Kafka (Producer)
│   │   ├── producer_air.ipynb
│   │   └── producer_traffy.ipynb
│   ├── ingestion/          # Notebooks รับข้อมูล (Consumer) & ประมวลผล (Spark)
│   │   ├── consumer_air.ipynb
│   │   ├── consumer_traffy.ipynb
│   │   ├── spark_air.ipynb
│   │   └── spark_traffy.ipynb
│   └── visualization/      # Streamlit Dashboard
│       ├── streamlit.ipynb # (สำหรับ Test Code)
│       └── my_app.py       # (ไฟล์หลักสำหรับรัน Dashboard)
└── README.md
````

-----

## 🚀 Setup & Run Instructions

### Step 1: Start Infrastructure (Docker)

เริ่มต้นระบบเซิร์ฟเวอร์จำลอง (Kafka, Zookeeper, Spark)

  * **Action:** เปิดไฟล์ **`setup.ipynb`** ที่ Root Project แล้วกด **Run All**
  * **What it does:** สคริปต์จะสั่งงาน Docker Compose ให้สร้างและรัน Container ที่จำเป็นทั้งหมด + โหลดข้อมูลตั้งต้นเข้าสู่ Redis
  * **Output:** คุณจะเห็น Container ชื่อ `traffy-redis`, `traffy-kafka`, `traffy-zookeeper`, และ `traffy-spark` สถานะเป็น **Running** ใน Docker Desktop

-----

### Step 2: Data Source (Producer)

ดึงข้อมูลจากแหล่งต่างๆ และส่งเข้าสู่ท่อส่งข้อมูล (Kafka)

  * **Action:**
    1.  รันไฟล์ **`src/data_source/producer_traffy.ipynb`** (ส่งข้อมูลร้องเรียน)
    2.  รันไฟล์ **`src/data_source/producer_air.ipynb`** (ส่งข้อมูล PM2.5)
  * **What it does:** โหลดข้อมูลจาก Redis หรือยิง API เพื่อดึงข้อมูล แล้วส่ง JSON Message เข้าไปรอใน Kafka Topic
  * **Output:** ข้อมูลเข้าไปรออยู่ใน Kafka Topic (`traffy_data`, `air_data`)

-----

### Step 3: Ingestion (Consumer -\> Raw CSV)

รับข้อมูลจาก Kafka มาเก็บลงไฟล์ CSV เพื่อเป็นข้อมูลดิบ (Bronze Layer)

  * **Action:**
    1.  รันไฟล์ **`src/ingestion/consumer_traffy.ipynb`**
    2.  รันไฟล์ **`src/ingestion/consumer_air.ipynb`**
  * **What it does:** โปรแกรมจะฟัง Kafka ตลอดเวลา เมื่อมีข้อมูลมา จะบันทึกลงไฟล์ CSV ทันที (ยังไม่ Clean)
  * **Output:** ได้ไฟล์ `data/raw/bangkok_traffy_raw.csv` และ `data/raw/bangkok_air_raw.csv`

-----

### Step 4: Processing (Spark -\> Cleaned CSV)

ใช้ Spark Clean ข้อมูลและซ่อมแซมส่วนที่เสียหาย (Silver Layer)

  * **Action:**
    1.  รันไฟล์ **`src/ingestion/spark_traffy.ipynb`**
    2.  รันไฟล์ **`src/ingestion/spark_air.ipynb`**
  * **What it does:**
      * ใช้ Spark อ่านไฟล์ Raw CSV (แบบ Multi-line เพื่อรองรับ Comment ยาวๆ)
      * **Imputation:** ซ่อมพิกัด GPS ที่หายไป โดย Join กับ `thailand_geography.csv`
      * **Cleaning:** ลบอักขระพิเศษ (`\n`, `\t`) และจัด Format วันที่
  * **Output:** ได้ไฟล์ข้อมูลพร้อมใช้งาน `data/cleansed/bangkok_traffy_clean.csv` และ `data/cleansed/bangkok_air_clean.csv`

-----

### Step 5: Visualization (Streamlit)

แสดงผลข้อมูลบน Dashboard

  * **Action:** เปิด Terminal (ที่ path หลักของ Project) แล้วรันคำสั่ง:
    ```bash
    streamlit run src/visualization/my_app.py
    ```
  * **What it does:** อ่านไฟล์ Cleaned CSV แล้วสร้างกราฟและแผนที่ Interactive
  * **Output:** เว็บ Dashboard ที่ `http://localhost:8501`

-----

## 🔌 How to Connect VS Code to Spark (Remote Kernel)

เพื่อให้รันโค้ด Spark (Step 4) ใน VS Code ได้สะดวกและรันบน Docker ได้จริง ให้ทำดังนี้:

1.  **Get Token:**

      * เปิด Terminal แล้วพิมพ์คำสั่ง: `docker logs traffy-spark`
      * ก๊อปปี้ลิงก์ที่หน้าตาแบบนี้: `http://127.0.0.1:8888/lab?token=xxxx...`

2.  **Connect in VS Code:**

      * เปิดไฟล์ `.ipynb` ที่ต้องการรัน (เช่น `spark_traffy.ipynb`)
      * คลิกที่ปุ่มเลือก Kernel มุมขวาบน (เช่น **Select Kernel** หรือ **Python 3**)
      * เลือกเมนู **Select Another Kernel...**
      * เลือก **Existing Jupyter Server...**
      * เลือก **Enter the URL of the running Jupyter Server**
      * **วางลิงก์** ที่ก๊อปมา แล้วกด Enter
      * ตั้งชื่อ Server (เช่น `Docker Spark`)

3.  **Ready:** ตอนนี้ VS Code ของคุณเชื่อมต่อกับ Spark ใน Docker เรียบร้อยแล้ว\!

-----

### 👨‍💻 Author

Project for Data Science Course